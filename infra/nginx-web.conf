server {
  # Web 容器内部只监听 80，外部端口由 docker-compose.yml 决定。
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    # 前端是单页应用：找不到真实文件时回退到 index.html，由 React 接管路由。
    try_files $uri /index.html;
  }
}
